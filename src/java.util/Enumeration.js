/**
* 双端队列
**/
var DequeManage = {
    options:{},
    init:function (){

    }，
    bindEvent:function(){

    },
    /**
    * 返回Boolean
    **/
    add:function(Object o1){
        var result = new Boolean();
        return result;
    },
    /**
    *
    **/
    addFirst:function(Object o1){

    },
    /**
    *
    **/
    addLast:function(Object o1){

    },
    /**
    *
    **/
    contains:function(){

    },
    /**
    *
    **/
   descendingIterator:function(){

   },
   /**
   *
   **/
   element:function(){

   },
   /**
   *
   **/
   getFirst:function(){

   },
   /**
   *
   **/
   getLast:function(){

   },
   /**
   *
   **/
   iterator:function(){

   },
   /**
   *
   **/
   offer:function(){

   },
   /**
   * 插入：双端队列头部
   **/
   offerFirst:function(){

   },
   /**
   * 插入：双端队列尾部
   **/
   offerLast:function(){

   },
   /**
   * 获取：双端队列头部，但是不删除。如果队列为空，返回null
   **/
   peek:function(){

   },
   /**
   * 获取：双端队列的第一个元素，但是不删除。如果队列为空，返回null
   **/
   peekFirst:function(){

   },
   /**
   * 获取：双端队列的最后一个元素，但是不删除。如果队列为空，返回为null
   **/
   peekLast:function(){

   },
   /**
   * 获取并移除双端队列头部。如果队列为空，返回null.
   **/
   poll:function(){

   },
   /**
   * 获取并移除双端队列头部。如果队列为空，返回null.
   **/
   pollFirst:function(){

   },
   /**
   * 获取并移除双端队列头部。如果队列为空，返回null.
   **/
   pollLast:function(){

   },
   /**
   * 从此双端队列表示的堆栈中，弹出第一个元素
   **/
   pop:function(){

   },
   /**
   * 将一个元素推入此双端队列表示的堆栈中（换句话说：此双端队列的头部）
   **/
   push(Object o1){

   },
   /**
   * 获取并移除此双端队列所表示的队列的头部。
   **/
   remove:function(){

   },
   /**
   * 从此双端队列中移除第一次出现的指定元素
   **/
   remove:function(Object o1){

   },
   /**
   * 获取并移除此双端队列的第一个元素
   **/
   removeFirst:function(){

   },
   /**
   * 从此双端队列中移除第一次出现的指定元素
   **/
   removeFirstOccurrence:function(Object o1){

   }
   /**
   * 获取并移除此双端队列最后一个元素
   **/
   removeLast:function(){

   },
   /**
   * 从此双端队列中移除最后一次出现的指定元素
   **/
   removeLastOccurrence:function(Object o1){

   },
   /**
   * 返回此双端队列的元素数
   **/
   size:function(){
     var num = new Number(0);
     return num;
   }
   
}
